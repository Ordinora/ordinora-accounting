import { notFound } from "next/navigation";import { AppShell } from "@/components/app-shell";import { db } from "@/lib/db";import { requireActiveTenant } from "@/lib/session";import { updateAccount } from "../../actions";import { AccountForm } from "../../account-form";
export const dynamic="force-dynamic";export default async function EditAccountPage({params}:{params:Promise<{id:string}>}){const{id}=await params;const{user,tenants,active}=await requireActiveTenant();const account=await db.account.findFirst({where:{id,tenantId:active.id}});if(!account)notFound();return <AppShell user={{displayName:user.displayName,email:user.email,role:user.staffRole?.replaceAll("_"," ")??"STAFF",firmName:user.firm.name}} tenants={tenants} activeTenant={active} pageTitle={`Edit ${account.code}`} pageDescription="Update or safely deactivate this ledger account"><main className="module-page form-page"><div className="form-notice"><strong>No permanent deletion</strong><span>Deactivate unused accounts to preserve transaction and audit history.</span></div><AccountForm action={updateAccount} account={account}/></main></AppShell>}
