import { AppShell } from "@/components/app-shell";import { requireActiveTenant } from "@/lib/session";import { createAccount } from "../actions";import { AccountForm } from "../account-form";
export const dynamic="force-dynamic";export default async function NewAccountPage(){const{user,tenants,active}=await requireActiveTenant();return <AppShell user={{displayName:user.displayName,email:user.email,role:user.staffRole?.replaceAll("_"," ")??"STAFF",firmName:user.firm.name}} tenants={tenants} activeTenant={active} pageTitle="New Account" pageDescription="Add an account to the selected client’s ledger"><main className="module-page form-page"><AccountForm action={createAccount}/></main></AppShell>}
