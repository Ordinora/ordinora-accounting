import{AppShell}from"@/components/app-shell";import{RetailSalesImportForm}from"@/components/retail-sales-import-form";import{db}from"@/lib/db";import{requireActiveTenant}from"@/lib/session";import{importRetailSales}from"./actions";export const dynamic="force-dynamic";export default async function Page(){const{user,tenants,active}=await requireActiveTenant(),accounts=await db.account.findMany({where:{tenantId:active.id,isActive:true,type:"ASSET",reportingClassification:"Cash and cash equivalents"},orderBy:{code:"asc"}});return <AppShell user={{displayName:user.displayName,email:user.email,role:user.staffRole?.replaceAll("_"," ")??"STAFF",firmName:user.firm.name}} tenants={tenants} activeTenant={active} pageTitle="Import Retail Sales Summary" pageDescription="Post item-level POS summaries without entering individual receipts"><main className="module-page form-page"><RetailSalesImportForm action={importRetailSales} accounts={accounts.map(x=>({id:x.id,label:`${x.code} — ${x.name}`}))}/></main></AppShell>}
