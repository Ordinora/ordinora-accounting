import Link from "next/link";import { AppShell } from "@/components/app-shell";import { db } from "@/lib/db";import { requireActiveTenant } from "@/lib/session";import { createInventoryItem } from "../../actions";export const dynamic="force-dynamic";
export default async function Page(){const{user,tenants,active}=await requireActiveTenant();const accounts=await db.account.findMany({where:{tenantId:active.id,isActive:true},orderBy:{code:"asc"}});const assets=accounts.filter(a=>a.type==="ASSET"),revenue=accounts.filter(a=>a.type==="REVENUE"),expenses=accounts.filter(a=>a.type==="EXPENSE");return <AppShell user={{displayName:user.displayName,email:user.email,role:user.staffRole?.replaceAll("_"," ")??"STAFF",firmName:user.firm.name}} tenants={tenants} activeTenant={active} pageTitle="New Inventory Item" pageDescription="Configure an item and its accounting mappings"><main className="module-page form-page"><form action={createInventoryItem} className="form-panel"><section className="form-section"><div className="section-heading"><h2>Item details</h2><p>Each item has permanent inventory, revenue, and cost-of-sales account mappings.</p></div><div className="form-grid"><label>SKU / item code<input name="sku" required placeholder="ITEM-001"/></label><label>Item name<input name="name" required/></label><label>Unit of measure<input name="unitName" defaultValue="unit" required/></label><label className="span-2">Description<input name="description"/></label></div></section><section className="form-section"><div className="section-heading"><h2>Accounting mappings</h2></div><div className="form-grid"><label>Inventory asset account<select name="inventoryAccountId" required><option value="">Select asset account</option>{assets.map(a=><option key={a.id} value={a.id}>{a.code} — {a.name}</option>)}</select></label><label>Sales revenue account<select name="revenueAccountId" required><option value="">Select revenue account</option>{revenue.map(a=><option key={a.id} value={a.id}>{a.code} — {a.name}</option>)}</select></label><label>Cost-of-sales account<select name="cogsAccountId" required><option value="">Select expense account</option>{expenses.map(a=><option key={a.id} value={a.id}>{a.code} — {a.name}</option>)}</select></label></div></section><div className="form-actions"><Link href="/inventory" className="button-secondary">Cancel</Link><button className="button-primary">Create item</button></div></form></main></AppShell>}
