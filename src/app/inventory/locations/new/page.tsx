import Link from "next/link";import { AppShell } from "@/components/app-shell";import { requireActiveTenant } from "@/lib/session";import { createInventoryLocation } from "../../actions";export const dynamic="force-dynamic";
export default async function Page(){const{user,tenants,active}=await requireActiveTenant();return <AppShell user={{displayName:user.displayName,email:user.email,role:user.staffRole?.replaceAll("_"," ")??"STAFF",firmName:user.firm.name}} tenants={tenants} activeTenant={active} pageTitle="New Stock Location" pageDescription="Create a warehouse, shop, or branch stock location"><main className="module-page form-page"><form action={createInventoryLocation} className="form-panel"><section className="form-section"><div className="section-heading"><h2>Stock location</h2><p>Locations keep quantities separate for future transfers and branch reporting.</p></div><div className="form-grid"><label>Location code<input name="code" required placeholder="MAIN"/></label><label>Location name<input name="name" required placeholder="Main Shop"/></label><label className="span-2">Branch label<input name="branchLabel" placeholder="Bandar Seri Begawan branch"/></label></div></section><div className="form-actions"><Link href="/inventory" className="button-secondary">Cancel</Link><button className="button-primary">Create location</button></div></form></main></AppShell>}
