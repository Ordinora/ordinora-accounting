import Link from "next/link";import { AppShell } from "@/components/app-shell";import { AutoReferenceField } from "@/components/auto-reference-field";import { db } from "@/lib/db";import { requireActiveTenant } from "@/lib/session";import { postTransfer } from "../../operations/actions";export const dynamic="force-dynamic";
export default async function Page(){const{user,tenants,active}=await requireActiveTenant();const[items,locations]=await Promise.all([db.inventoryItem.findMany({where:{tenantId:active.id,isActive:true},orderBy:{sku:"asc"}}),db.inventoryLocation.findMany({where:{tenantId:active.id,isActive:true},orderBy:{code:"asc"}})]);const today=new Date().toISOString().slice(0,10);return <AppShell user={{displayName:user.displayName,email:user.email,role:user.staffRole?.replaceAll("_"," ")??"STAFF",firmName:user.firm.name}} tenants={tenants} activeTenant={active} pageTitle="New Stock Transfer" pageDescription="Move inventory between locations without changing total value"><main className="module-page form-page"><form action={postTransfer} className="form-panel"><section className="form-section"><div className="section-heading"><h2>Transfer details</h2><p>Weighted-average cost moves with the stock. No profit-and-loss journal is created.</p></div><div className="form-grid"><AutoReferenceField example="ITR-2026-0001"/><label>Date<input name="operationDate" type="date" defaultValue={today} required/></label><label>Inventory item<select name="itemId" required><option value="">Select item</option>{items.map(i=><option key={i.id} value={i.id}>{i.sku} — {i.name}</option>)}</select></label><label>Source location<select name="sourceLocationId" required><option value="">Select location</option>{locations.map(l=><option key={l.id} value={l.id}>{l.code} — {l.name}</option>)}</select></label><label>Destination location<select name="destinationLocationId" required><option value="">Select location</option>{locations.map(l=><option key={l.id} value={l.id}>{l.code} — {l.name}</option>)}</select></label><label>Quantity<input name="quantity" required/></label><label className="span-2">Reason<input name="reason" required/></label></div></section><div className="form-actions"><Link href="/inventory" className="button-secondary">Cancel</Link><button className="button-primary">Post transfer</button></div></form></main></AppShell>}


