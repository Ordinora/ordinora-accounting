import Link from"next/link";import{Plus}from"lucide-react";import{AppShell}from"@/components/app-shell";import{db}from"@/lib/db";import{requireActiveTenant}from"@/lib/session";export const dynamic="force-dynamic";const money=(c:string,v:unknown)=>`${c} ${Number(v).toLocaleString("en-BN",{minimumFractionDigits:2,maximumFractionDigits:2})}`;
export default async function Page(){const{user,tenants,active}=await requireActiveTenant(),employees=await db.employee.findMany({where:{tenantId:active.id},include:{_count:{select:{payrollEntries:true}}},orderBy:[{status:"asc"},{fullName:"asc"}]});return <AppShell user={{displayName:user.displayName,email:user.email,role:user.staffRole?.replaceAll("_"," ")??"STAFF",firmName:user.firm.name}} tenants={tenants} activeTenant={active} pageTitle="Employees" pageDescription="Payroll employment and lifecycle register"><main className="module-page"><header className="module-header"><div><p className="eyebrow">{active.legalName.toUpperCase()}</p><h2>Employee register</h2><p>Salary changes affect future payroll only.</p></div><Link href="/payroll/employees/new" className="button-primary"><Plus size={16}/>New employee</Link></header><section className="surface-card table-card"><div className="data-table-wrap"><table className="data-table"><thead><tr><th>Employee</th><th>Name</th><th>Department</th><th>Employment</th><th>Status</th><th className="numeric">Basic salary</th><th className="numeric">Payroll records</th><th></th></tr></thead><tbody>{employees.map(e=><tr key={e.id}><td><strong>{e.employeeNumber}</strong></td><td>{e.fullName}</td><td>{e.department??"—"}</td><td>{e.employmentStart.toLocaleDateString("en-BN")} {e.employmentEnd?`– ${e.employmentEnd.toLocaleDateString("en-BN")}`:"– present"}</td><td>{e.status.replaceAll("_"," ")}</td><td className="numeric">{money(active.defaultCurrency,e.basicSalary)}</td><td className="numeric">{e._count.payrollEntries}</td><td><Link className="record-link" href={`/payroll/employees/${e.id}/edit`}>Edit</Link></td></tr>)}{!employees.length&&<tr><td colSpan={8} className="table-empty">No employees have been added.</td></tr>}</tbody></table></div></section></main></AppShell>}
