import Link from "next/link";
import { calculateDashboardBalances } from "@/lib/dashboard-calculations";
import { db } from "@/lib/db";
import { canClientViewFinancials, requireClient } from "@/lib/session";
import { portalLogout } from "./actions";

export const dynamic="force-dynamic";
async function LivePostingTimestamp({tenantId}:{tenantId:string}){const latest=await db.journal.findFirst({where:{tenantId,status:{in:["POSTED","REVERSED"]}},orderBy:{updatedAt:"desc"},select:{updatedAt:true}});return <p className="portal-freshness">Last posted update: {latest?.updatedAt.toLocaleString("en-BN")??"No posted entries"}</p>}
const money=(currency:string,value:number)=>`${currency} ${value.toLocaleString("en-US",{minimumFractionDigits:2,maximumFractionDigits:2})}`;

export default async function ClientPortalPage(){const user=await requireClient(),tenant=user.tenant!,financialAccess=canClientViewFinancials(user.clientRole);const[reports,lines]=await Promise.all([financialAccess?db.reportVersion.findMany({where:{tenantId:tenant.id,state:"PUBLISHED"},include:{period:true},orderBy:{publishedAt:"desc"}}):Promise.resolve([]),financialAccess&&tenant.reportMode==="LIVE_POSTED_AND_PUBLISHED"?db.journalLine.findMany({where:{journal:{tenantId:tenant.id,status:{in:["POSTED","REVERSED"]}}},include:{account:true}}):Promise.resolve([])]);const balances=calculateDashboardBalances(lines),revenue=lines.filter(line=>line.account.type==="REVENUE").reduce((sum,line)=>sum+Number(line.credit)-Number(line.debit),0);const cards={cash:["Cash & bank",balances.cashAndBank],revenue:["Revenue",revenue],receivables:["Accounts receivable",balances.receivables],payables:["Accounts payable",balances.payables],profit:["Net profit",balances.netProfit]} as const;return <main className="portal-page"><header className="portal-header"><div><strong>Ordinora</strong><span>Client Portal</span></div><div><Link href="/portal/documents">Documents</Link><Link href="/portal/questions">Questions</Link><span>{user.displayName}</span><form action={portalLogout}><button className="button-secondary">Sign out</button></form></div></header><section className="portal-content"><div className="dashboard-intro"><div><p className="eyebrow">{tenant.legalName.toUpperCase()}</p><h2>Financial overview</h2><p>Income and profit use accrual accounting from posted entries.</p>{financialAccess&&tenant.reportMode==="LIVE_POSTED_AND_PUBLISHED"&&<LivePostingTimestamp tenantId={tenant.id}/>}</div><div className="dashboard-controls"><span className="status-badge active">{tenant.reportMode==="PUBLISHED_ONLY"?"PUBLISHED REPORTS":"LIVE POSTED"}</span></div></div>{financialAccess&&tenant.reportMode==="LIVE_POSTED_AND_PUBLISHED"&&<div className="kpi-grid">{tenant.enabledDashboardCards.map(key=>{const card=cards[key as keyof typeof cards];if(!card)return null;const[label,value]=card;return <Link href={`/portal/live/${key}`} className="kpi-card portal-kpi-link" key={key}><div className="kpi-label"><span>{label}</span></div><strong>{money(tenant.defaultCurrency,value)}</strong><p>{key==="profit"?"Accrual basis · open posted drill-down":"Open posted drill-down"}</p></Link>})}</div>}<section className="surface-card table-card portal-report-list"><div className="card-header"><div><h3>Published reports</h3><p>Reports specifically released by your accountant.</p></div></div><div className="data-table-wrap"><table className="data-table"><thead><tr><th>Report</th><th>Period</th><th>Version</th><th>Published</th><th></th></tr></thead><tbody>{reports.map(report=><tr key={report.id}><td><strong>{report.reportType.replaceAll("_"," ")}</strong></td><td>{report.period.name}</td><td>{report.version}</td><td>{report.publishedAt?.toLocaleDateString("en-BN")??"—"}</td><td><Link className="table-action" href={`/portal/reports/${report.id}`}>Open</Link></td></tr>)}{!reports.length&&<tr><td colSpan={5} className="table-empty">No reports have been published yet.</td></tr>}</tbody></table></div></section></section></main>}
