import{createPurchaseOrder}from"@/app/purchases/procurement-actions";import{AppShell}from"@/components/app-shell";import{ProcurementForm}from"@/components/procurement-form";import{db}from"@/lib/db";import{isPermittedPurchaseAccount,purchaseAccountTypes}from"@/lib/purchase-account-policy";import{requireActiveTenant}from"@/lib/session";
export default async function Page(){const{user,tenants,active}=await requireActiveTenant(),[suppliers,purchaseAccounts,items,locations]=await Promise.all([db.supplier.findMany({where:{tenantId:active.id,isActive:true},orderBy:{name:"asc"}}),db.account.findMany({where:{tenantId:active.id,isActive:true,type:{in:purchaseAccountTypes}},include:{_count:{select:{inventoryAssetItems:true}}},orderBy:{code:"asc"}}),db.inventoryItem.findMany({where:{tenantId:active.id,isActive:true},orderBy:{sku:"asc"}}),db.inventoryLocation.findMany({where:{tenantId:active.id,isActive:true},orderBy:{code:"asc"}})]),accounts=purchaseAccounts.filter(isPermittedPurchaseAccount);return <AppShell user={{displayName:user.displayName,email:user.email,role:user.staffRole?.replaceAll("_"," ")||"STAFF",firmName:user.firm.name}} tenants={tenants} activeTenant={active} pageTitle="New Purchase Order" pageDescription="Create a non-ledger supplier commitment"><main className="module-page form-page"><ProcurementForm kind="order" action={createPurchaseOrder} suppliers={suppliers} accounts={accounts} items={items} locations={locations}/></main></AppShell>}
