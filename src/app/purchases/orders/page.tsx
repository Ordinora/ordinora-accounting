import Link from"next/link";import{Plus}from"lucide-react";import{AppShell}from"@/components/app-shell";import{db}from"@/lib/db";import{requireActiveTenant}from"@/lib/session";
export const dynamic="force-dynamic";export default async function Page(){const{user,tenants,active}=await requireActiveTenant(),orders=await db.purchaseOrder.findMany({where:{tenantId:active.id},include:{supplier:true,quotation:true,supplierBills:true},orderBy:{orderDate:"desc"}});return <AppShell user={{displayName:user.displayName,email:user.email,role:user.staffRole?.replaceAll("_"," ")||"STAFF",firmName:user.firm.name}} tenants={tenants} activeTenant={active} pageTitle="Purchase Orders" pageDescription="Supplier commitments, receipts, and billing status"><main className="module-page"><header className="module-header"><div><p className="eyebrow">{active.legalName.toUpperCase()}</p><h2>Purchase orders</h2><p>Track approval, partial receipt, and supplier billing without duplicate posting.</p></div><Link href="/purchases/orders/new" className="button-primary"><Plus size={16}/>New purchase order</Link></header><section className="surface-card table-card"><div className="data-table-wrap"><table className="data-table"><thead><tr><th>Reference</th><th>Supplier</th><th>Date</th><th>Expected</th><th>Status</th><th className="numeric">Total</th><th>Bills</th><th/></tr></thead><tbody>{orders.map(o=><tr key={o.id}><td><Link className="record-link" href={`/purchases/orders/${o.id}`}>{o.reference}</Link></td><td>{o.supplier.name}</td><td>{o.orderDate.toLocaleDateString("en-BN")}</td><td>{o.expectedDate?.toLocaleDateString("en-BN")||"—"}</td><td><span className={`status-badge ${o.status.toLowerCase()}`}>{o.status.replaceAll("_"," ")}</span></td><td className="numeric">{o.currency} {Number(o.foreignTotal).toFixed(2)}</td><td>{o.supplierBills.length}</td><td><Link className="table-action" href={`/purchases/orders/${o.id}`}>View</Link></td></tr>)}{!orders.length&&<tr><td colSpan={8} className="table-empty">No purchase orders yet.</td></tr>}</tbody></table></div></section></main></AppShell>}
