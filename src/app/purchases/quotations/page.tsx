import Link from"next/link";import{Plus}from"lucide-react";import{AppShell}from"@/components/app-shell";import{db}from"@/lib/db";import{requireActiveTenant}from"@/lib/session";
export const dynamic="force-dynamic";export default async function Page(){const{user,tenants,active}=await requireActiveTenant(),quotes=await db.supplierQuotation.findMany({where:{tenantId:active.id},include:{supplier:true,purchaseOrder:true},orderBy:[{comparisonReference:"asc"},{foreignTotal:"asc"}]});return <AppShell user={{displayName:user.displayName,email:user.email,role:user.staffRole?.replaceAll("_"," ")||"STAFF",firmName:user.firm.name}} tenants={tenants} activeTenant={active} pageTitle="Supplier Quotations" pageDescription="Compare competing supplier offers"><main className="module-page"><header className="module-header"><div><p className="eyebrow">{active.legalName.toUpperCase()}</p><h2>Supplier quotation comparison</h2><p>Use the same comparison reference for offers covering the same requirement. Lowest totals appear first within each group.</p></div><Link href="/purchases/quotations/new" className="button-primary"><Plus size={16}/>New supplier quotation</Link></header><section className="surface-card table-card"><div className="data-table-wrap"><table className="data-table"><thead><tr><th>Comparison</th><th>Quote</th><th>Supplier</th><th>Date</th><th>Status</th><th className="numeric">Total</th><th>Purchase order</th><th/></tr></thead><tbody>{quotes.map(q=><tr key={q.id}><td><strong>{q.comparisonReference}</strong></td><td><Link className="record-link" href={`/purchases/quotations/${q.id}`}>{q.reference}</Link></td><td>{q.supplier.name}</td><td>{q.quoteDate.toLocaleDateString("en-BN")}</td><td><span className={`status-badge ${q.status.toLowerCase()}`}>{q.status}</span></td><td className="numeric">{q.currency} {Number(q.foreignTotal).toFixed(2)}</td><td>{q.purchaseOrder?.reference||"—"}</td><td><Link className="table-action" href={`/purchases/quotations/${q.id}`}>View</Link></td></tr>)}{!quotes.length&&<tr><td colSpan={8} className="table-empty">No supplier quotations yet.</td></tr>}</tbody></table></div></section></main></AppShell>}
