import Link from"next/link";import{Download}from"lucide-react";import{AppShell}from"@/components/app-shell";import{bankAccountSummary}from"@/lib/cash-account-reports";import{requireActiveTenant}from"@/lib/session";export const dynamic="force-dynamic";const date=(v:string|undefined,f:Date)=>{const d=v?new Date(`${v}T00:00:00.000Z`):f;return Number.isNaN(d.getTime())?f:d},shown=(d:Date)=>d.toLocaleDateString("en-GB",{timeZone:"UTC"}),money=(c:string,v:{toString():string})=>`${c} ${Number(v.toString()).toLocaleString("en-US",{minimumFractionDigits:2,maximumFractionDigits:2})}`;
export default async function Page({searchParams}:{searchParams:Promise<{from?:string;to?:string}>}){const q=await searchParams,{user,tenants,active}=await requireActiveTenant(),now=new Date(),from=date(q.from,new Date(Date.UTC(now.getUTCFullYear(),0,1))),to=date(q.to,now),rows=await bankAccountSummary(active.id,from,to),key=`from=${from.toISOString().slice(0,10)}&to=${to.toISOString().slice(0,10)}`;return <AppShell user={{displayName:user.displayName,email:user.email,role:user.staffRole?.replaceAll("_"," ")??"STAFF",firmName:user.firm.name}} tenants={tenants} activeTenant={active} pageTitle="Bank Account Summary" pageDescription="Opening, receipts, payments, and closing balance by cash or bank account"><main className="module-page"><div className="detail-toolbar"><Link href="/reports" className="back-link">← Report library</Link></div><header className="module-header"><div><p className="eyebrow">{active.legalName.toUpperCase()}</p><h2>Bank Account Summary</h2><p>{shown(from)} to {shown(to)} · Posted cash and bank journals</p></div><div className="report-actions"><form className="report-filter"><label>From<input name="from" type="date" defaultValue={from.toISOString().slice(0,10)}/></label><label>To<input name="to" type="date" defaultValue={to.toISOString().slice(0,10)}/></label><button className="button-secondary">Update</button></form><Link className="button-secondary" href={`/reports/bank-account-summary/pdf?${key}`}><Download size={16}/>Export PDF</Link></div></header><section className="surface-card table-card"><div className="data-table-wrap"><table className="data-table"><thead><tr><th>Code</th><th>Cash / bank account</th><th className="numeric">Opening</th><th className="numeric">Receipts</th><th className="numeric">Payments</th><th className="numeric">Closing</th><th className="numeric">Entries</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.code}</td><td>{x.name}</td><td className="numeric">{money(active.defaultCurrency,x.opening)}</td><td className="numeric">{money(active.defaultCurrency,x.receipts)}</td><td className="numeric">{money(active.defaultCurrency,x.payments)}</td><td className="numeric"><strong>{money(active.defaultCurrency,x.closing)}</strong></td><td className="numeric">{x.transactions}</td></tr>)}{!rows.length&&<tr><td colSpan={7}>No cash or bank activity for this period.</td></tr>}</tbody></table></div></section></main></AppShell>}
