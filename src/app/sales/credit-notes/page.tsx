import Link from"next/link";import{Plus}from"lucide-react";import{AppShell}from"@/components/app-shell";import{SourceRecordActions}from"@/components/source-record-actions";import{db}from"@/lib/db";import{requireActiveTenant}from"@/lib/session";export const dynamic="force-dynamic";export default async function Page(){const{user,tenants,active}=await requireActiveTenant();const notes=await db.salesCreditNote.findMany({where:{tenantId:active.id},include:{customer:true,invoice:true},orderBy:{creditDate:"desc"}});return <AppShell user={{displayName:user.displayName,email:user.email,role:user.staffRole?.replaceAll("_"," ")??"STAFF",firmName:user.firm.name}} tenants={tenants} activeTenant={active} pageTitle="Sales Credit Notes" pageDescription="Customer returns and invoice reductions"><main className="module-page"><header className="module-header"><div><h2>Sales credit notes</h2><p>Credits remain linked to their original invoices and journals.</p></div><Link href="/sales/credit-notes/new" className="button-primary"><Plus size={16}/>New credit note</Link></header><section className="surface-card table-card"><div className="data-table-wrap"><table className="data-table"><thead><tr><th>Reference</th><th>Original invoice</th><th>Customer</th><th>Date</th><th className="numeric">Total</th><th aria-label="Options"></th></tr></thead><tbody>{notes.map(n=><tr key={n.id}><td><strong>{n.reference}</strong></td><td>{n.invoice.reference}</td><td>{n.customer.name}</td><td>{n.creditDate.toLocaleDateString("en-BN")}</td><td className="numeric">{n.currency} {Number(n.foreignTotal).toFixed(2)}</td><td><SourceRecordActions editHref={`/sales/credit-notes/${n.id}/edit`} journalId={n.journalId}/></td></tr>)}{!notes.length&&<tr><td colSpan={6}>No sales credit notes yet.</td></tr>}</tbody></table></div></section></main></AppShell>}
