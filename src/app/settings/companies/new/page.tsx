import Link from "next/link"; import { ArrowLeft } from "lucide-react"; import { AppShell } from "@/components/app-shell"; import { CompanyForm } from "@/components/company-form"; import { requireActiveTenant } from "@/lib/session"; export const dynamic = "force-dynamic";
export default async function Page() { const { user, tenants, active } = await requireActiveTenant(); return <AppShell user={{ displayName: user.displayName, email: user.email, role: user.staffRole?.replaceAll("_", " ") ?? "STAFF", firmName: user.firm.name }} tenants={tenants} activeTenant={active} pageTitle="New Company" pageDescription="Prepare a separate accounting file for a client"><main className="module-page form-page"><Link href="/settings/companies" className="back-link"><ArrowLeft size={15}/>Back to companies</Link><CompanyForm/></main></AppShell>; }
