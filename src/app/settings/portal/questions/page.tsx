import Link from "next/link";import { AppShell } from "@/components/app-shell";import { db } from "@/lib/db";import { requireActiveTenant } from "@/lib/session";import { assertCanAccessAdministrationFeature } from "@/lib/staff-access";export const dynamic="force-dynamic";
export default async function StaffQuestionsPage(){const{user,tenants,active}=await requireActiveTenant();assertCanAccessAdministrationFeature(user.staffRole,"client-questions");const questions=await db.question.findMany({where:{tenantId:active.id},include:{document:true,_count:{select:{messages:true}}},orderBy:[{status:"asc"},{createdAt:"desc"}]});return <AppShell user={{displayName:user.displayName,email:user.email,role:user.staffRole?.replaceAll("_"," ")??"STAFF",firmName:user.firm.name}} tenants={tenants} activeTenant={active} pageTitle="Client Questions" pageDescription="Client-visible conversations and private internal notes"><main className="module-page"><header className="module-header"><div><p className="eyebrow">{active.legalName.toUpperCase()}</p><h2>Question inbox</h2><p>Respond to client questions without exposing internal notes.</p></div></header><section className="surface-card table-card"><div className="data-table-wrap"><table className="data-table"><thead><tr><th>Subject</th><th>Document</th><th>Messages</th><th>Status</th><th>Opened</th><th></th></tr></thead><tbody>{questions.map(q=><tr key={q.id}><td><strong>{q.subject}</strong></td><td>{q.document?.originalName??"—"}</td><td>{q._count.messages}</td><td><span className={`status-badge ${q.status.toLowerCase()}`}>{q.status}</span></td><td>{q.createdAt.toLocaleString("en-BN")}</td><td><Link className="table-action" href={`/settings/portal/questions/${q.id}`}>Open</Link></td></tr>)}{!questions.length&&<tr><td colSpan={6} className="table-empty">No client questions have been received.</td></tr>}</tbody></table></div></section></main></AppShell>}


