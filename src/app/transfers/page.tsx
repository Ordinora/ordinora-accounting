import Link from"next/link";import{ArrowRightLeft,Plus}from"lucide-react";import{AppShell}from"@/components/app-shell";import{SourceRecordActions}from"@/components/source-record-actions";import{db}from"@/lib/db";import{requireActiveTenant}from"@/lib/session";export const dynamic="force-dynamic";export default async function Page(){const{user,tenants,active}=await requireActiveTenant();const rows=await db.interAccountTransfer.findMany({where:{tenantId:active.id},include:{sourceAccount:true,destinationAccount:true},orderBy:{transferDate:"desc"}});return <AppShell user={{displayName:user.displayName,email:user.email,role:user.staffRole?.replaceAll("_"," ")??"STAFF",firmName:user.firm.name}} tenants={tenants} activeTenant={active} pageTitle="Inter-Account Transfers" pageDescription="Movements between bank and cash accounts"><main className="module-page"><header className="module-header"><div><p className="eyebrow">{active.legalName.toUpperCase()}</p><h2>Inter-account transfers</h2><p>Each transfer credits the source account and debits the destination account.</p></div><Link href="/transfers/new" className="button-primary"><Plus size={16}/>New transfer</Link></header><section className="surface-card table-card"><div className="data-table-wrap"><table className="data-table"><thead><tr><th>Reference</th><th>Date</th><th>Movement</th><th>Description</th><th className="numeric">Paid</th><th className="numeric">Received</th><th aria-label="Options"></th></tr></thead><tbody>{rows.map(row=><tr key={row.id}><td><strong>{row.reference}</strong></td><td>{row.transferDate.toLocaleDateString("en-BN")}</td><td><span className="record-link">{row.sourceAccount.name} <ArrowRightLeft size={13}/> {row.destinationAccount.name}</span></td><td>{row.description||"—"}</td><td className="numeric">{row.sourceCurrency} {Number(row.sourceAmount).toFixed(2)}</td><td className="numeric">{row.destinationCurrency} {Number(row.destinationAmount).toFixed(2)}</td><td><SourceRecordActions editHref={`/transfers/${row.id}/edit`} journalId={row.journalId}/></td></tr>)}{!rows.length&&<tr><td colSpan={7} className="table-empty">No inter-account transfers have been recorded.</td></tr>}</tbody></table></div></section></main></AppShell>}
