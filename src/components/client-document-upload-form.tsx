"use client";import { useActionState } from "react";import { Upload } from "lucide-react";import { uploadClientDocument } from "@/app/portal/documents/actions";
export function ClientDocumentUploadForm(){const[state,action,pending]=useActionState(uploadClientDocument,{});return <form action={action} className="client-document-upload"><label>PDF or image<input name="document" type="file" accept=".pdf,.jpg,.jpeg,.png,application/pdf,image/jpeg,image/png" required/></label><p className="upload-help">Maximum 10 MB. PDF, JPG and PNG files only.</p>{state.error&&<div className="form-error" role="alert">{state.error}</div>}{state.success&&<div className="form-success" role="status">{state.success}</div>}<button className="button-primary" disabled={pending}><Upload size={15}/>{pending?"Uploading…":"Send to accountant"}</button></form>}
