import Link from "next/link";
import { Eye,Landmark,MoreHorizontal,Pencil,Trash2 } from "lucide-react";
export function CommercialRecordActions({kind,id,journalId}:{kind:"sale"|"purchase";id:string;journalId:string|null}){const base=kind==="sale"?"sales":"purchases";return <details className="transaction-actions"><summary aria-label="Transaction options" title="Transaction options"><MoreHorizontal size={19}/></summary><div className="transaction-action-menu"><Link href={`/${base}/${id}/edit`}><Eye size={15}/>View</Link><Link href={`/${base}/${id}/edit`}><Pencil size={15}/>Update</Link>{kind==="purchase"&&<Link href={`/purchases/${id}/fixed-assets`}><Landmark size={15}/>Register fixed asset</Link>}{journalId&&<Link href={`/journals/${journalId}#delete-transaction`} className="danger"><Trash2 size={15}/>Delete</Link>}</div></details>}
