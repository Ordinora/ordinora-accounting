import "server-only";import { AccountControlRole,JournalSource,Prisma,StaffRole } from "@prisma/client";import { parseMoneyToMinor } from "./accounting";import { requireControlAccount } from "./control-accounts";import { convertForeignToBase } from "./currency";import { issueInventory,receiveInventory } from "./inventory-ledger";import { isPermittedPurchaseAccount } from "./purchase-account-policy";import { runSerializableTransaction } from "./serializable-transaction";
type Actor={tenantId:string;userId:string;firmId:string;role:StaffRole|null};type CommercialLine={description:string;accountId:string;quantity:string;unitPrice:string;discountPercent?:string;itemId?:string;locationId?:string};const zero=new Prisma.Decimal(0);
function authorize(a:Actor){if(!a.role||!["SYSTEM_ADMIN","FIRM_ADMIN","ACCOUNTANT"].includes(a.role))throw new Error("Your role cannot post commercial transactions.");}
export async function postCommercialDocument(input:{kind:"SALE"|"PURCHASE";actor:Actor;partyId:string;reference:string;documentDate:Date;dueDate:Date;description:string;lines:CommercialLine[]}){authorize(input.actor);if(input.lines.length<1||input.lines.length>50)throw new Error("A document requires between 1 and 50 lines.");const raw=input.lines.map(line=>{const quantity=new Prisma.Decimal(line.quantity);if(quantity.lte(0)||quantity.decimalPlaces()>4)throw new Error("Quantities must be positive with no more than four decimal places.");const unitPrice=new Prisma.Decimal(parseMoneyToMinor(line.unitPrice).toString()).div(100),discountPercent=new Prisma.Decimal(line.discountPercent||0);if(discountPercent.lt(0)||discountPercent.gt(100))throw new Error("Discount percentages must be between 0 and 100.");const gross=quantity.mul(unitPrice).toDecimalPlaces(2,Prisma.Decimal.ROUND_HALF_UP),discountAmount=gross.mul(discountPercent).div(100).toDecimalPlaces(2,Prisma.Decimal.ROUND_HALF_UP),foreign=gross.sub(discountAmount);if(foreign.lte(0))throw new Error("Every document line must have a positive net amount.");return{...line,quantity,unitPrice,discountPercent,discountAmount,foreign};});const foreignTotal=raw.reduce((s,l)=>s.add(l.foreign),zero);
return runSerializableTransaction(async tx=>{const period=await tx.accountingPeriod.findFirst({where:{tenantId:input.actor.tenantId,status:"OPEN",startsOn:{lte:input.documentDate},endsOn:{gte:input.documentDate}},orderBy:{startsOn:"desc"}});if(!period)throw new Error("No open accounting period contains the document date. Open the required period and try again.");const tenant=await tx.tenant.findUniqueOrThrow({where:{id:input.actor.tenantId}});const party=input.kind==="SALE"?await tx.customer.findFirst({where:{id:input.partyId,tenantId:tenant.id,isActive:true}}):await tx.supplier.findFirst({where:{id:input.partyId,tenantId:tenant.id,isActive:true}});if(!party)throw new Error("Customer or supplier not found.");const currency=party.currencyCode;let exchangeRate=new Prisma.Decimal(1);if(currency!==tenant.defaultCurrency){const rate=await tx.exchangeRate.findFirst({where:{tenantId:tenant.id,currencyCode:currency,effectiveOn:{lte:input.documentDate}},orderBy:{effectiveOn:"desc"}});if(!rate)throw new Error(`No ${currency} exchange rate exists on or before the document date.`);exchangeRate=rate.rateToBase;}
const itemIds=[...new Set(raw.flatMap(l=>l.itemId?[l.itemId]:[]))];const items=await tx.inventoryItem.findMany({where:{tenantId:tenant.id,id:{in:itemIds},isActive:true}});if(items.length!==itemIds.length)throw new Error("Every selected inventory item must be active and belong to this client.");const locationIds=[...new Set(raw.flatMap(l=>l.itemId&&l.locationId?[l.locationId]:[]))];if(locationIds.length!==itemIds.length&&raw.some(l=>l.itemId&&!l.locationId))throw new Error("Select a stock location for every inventory item.");if(await tx.inventoryLocation.count({where:{tenantId:tenant.id,id:{in:locationIds},isActive:true}})!==locationIds.length)throw new Error("Every stock location must be active and belong to this client.");
const lines=raw.map(line=>{const item=line.itemId?items.find(i=>i.id===line.itemId):null;const accountId=item?(input.kind==="SALE"?item.revenueAccountId:item.inventoryAccountId):line.accountId;return{...line,item,accountId,base:convertForeignToBase(line.foreign,exchangeRate)}});const ordinary=lines.filter(l=>!l.item),ordinaryIds=[...new Set(ordinary.map(l=>l.accountId))],ordinaryAccounts=await tx.account.findMany({where:{tenantId:tenant.id,id:{in:ordinaryIds},isActive:true},select:{id:true,type:true,code:true,reportingClassification:true,isControlAccount:true,_count:{select:{inventoryAssetItems:true}}}});const accountsValid=input.kind==="SALE"?ordinaryAccounts.every(a=>a.type==="REVENUE"):ordinaryAccounts.every(isPermittedPurchaseAccount);if(ordinaryAccounts.length!==ordinaryIds.length||!accountsValid)throw new Error(input.kind==="SALE"?"Every non-inventory line must use an active revenue account.":"Every non-inventory purchase line must use an active expense or permitted asset account. Use inventory items for stock purchases.");const control=await requireControlAccount(tx,tenant.id,input.kind==="SALE"?AccountControlRole.TRADE_RECEIVABLES:AccountControlRole.TRADE_PAYABLES);const baseTotal=lines.reduce((s,l)=>s.add(l.base),zero);const common={tenantId:tenant.id,periodId:period.id,reference:input.reference,dueDate:input.dueDate,description:input.description,currency,exchangeRate,foreignTotal,baseTotal,createdById:input.actor.userId,postedAt:new Date()};const document=input.kind==="SALE"?await tx.salesInvoice.create({data:{...common,customerId:input.partyId,invoiceDate:input.documentDate,lines:{create:lines.map(l=>({revenueAccountId:l.accountId,inventoryItemId:l.item?.id,inventoryLocationId:l.item?l.locationId:null,description:l.description,quantity:l.quantity,unitPrice:l.unitPrice,discountPercent:l.discountPercent,discountAmount:l.discountAmount,lineTotal:l.foreign}))}}}):await tx.supplierBill.create({data:{...common,supplierId:input.partyId,billDate:input.documentDate,lines:{create:lines.map(l=>({expenseAccountId:l.accountId,inventoryItemId:l.item?.id,inventoryLocationId:l.item?l.locationId:null,description:l.description,quantity:l.quantity,unitPrice:l.unitPrice,discountPercent:l.discountPercent,discountAmount:l.discountAmount,lineTotal:l.foreign}))}}});
const inventoryJournal:{accountId:string;debit:Prisma.Decimal;credit:Prisma.Decimal;description:string}[]=[];for(const line of lines.filter(l=>l.item)){const item=line.item!,locationId=line.locationId!;if(input.kind==="PURCHASE"){const receipt=await receiveInventory(tx,{tenantId:tenant.id,costingMethod:tenant.inventoryCostingMethod,itemId:item.id,locationId,receivedOn:input.documentDate,quantity:line.quantity,totalValue:line.base,sourceType:"SupplierBill",sourceId:document.id});await tx.inventoryMovement.create({data:{tenantId:tenant.id,itemId:item.id,locationId,type:"PURCHASE",movementDate:input.documentDate,quantity:line.quantity,unitCost:receipt.receiptUnitCost,totalCost:line.base,reference:input.reference,sourceType:"SupplierBill",sourceId:document.id,createdById:input.actor.userId}});}else{const issued=await issueInventory(tx,{tenantId:tenant.id,costingMethod:tenant.inventoryCostingMethod,itemId:item.id,locationId,quantity:line.quantity});await tx.inventoryMovement.create({data:{tenantId:tenant.id,itemId:item.id,locationId,type:"SALE",movementDate:input.documentDate,quantity:line.quantity.neg(),unitCost:issued.unitCost,totalCost:issued.cost.neg(),reference:input.reference,sourceType:"SalesInvoice",sourceId:document.id,createdById:input.actor.userId}});inventoryJournal.push({accountId:item.cogsAccountId,debit:issued.cost,credit:zero,description:`COGS — ${item.name}`},{accountId:item.inventoryAccountId,debit:zero,credit:issued.cost,description:`Inventory issued — ${item.name}`});}}
const transactionLines=input.kind==="SALE"?[{accountId:control.id,debit:baseTotal,credit:zero,description:input.description},...lines.map(l=>({accountId:l.accountId,debit:zero,credit:l.base,description:l.description})),...inventoryJournal]:[...lines.map(l=>({accountId:l.accountId,debit:l.base,credit:zero,description:l.description})),{accountId:control.id,debit:zero,credit:baseTotal,description:input.description}];const source:JournalSource=input.kind==="SALE"?"SALES_INVOICE":"SUPPLIER_BILL";const journal=await tx.journal.create({data:{tenantId:tenant.id,periodId:period.id,reference:input.reference,description:input.description,accountingDate:input.documentDate,status:"POSTED",source,sourceId:document.id,createdById:input.actor.userId,approvedById:input.actor.userId,postedById:input.actor.userId,postedAt:new Date(),lines:{create:transactionLines}}});if(input.kind==="SALE")await tx.salesInvoice.update({where:{id:document.id},data:{journalId:journal.id}});else await tx.supplierBill.update({where:{id:document.id},data:{journalId:journal.id}});await tx.auditEvent.create({data:{firmId:input.actor.firmId,tenantId:tenant.id,actorId:input.actor.userId,actorKind:"STAFF",action:input.kind==="SALE"?"SALES_INVOICE_POSTED":"SUPPLIER_BILL_POSTED",entityType:input.kind==="SALE"?"SalesInvoice":"SupplierBill",entityId:document.id,newValues:{reference:input.reference,currency,foreignTotal:foreignTotal.toString(),baseTotal:baseTotal.toString(),inventoryLineCount:lines.filter(l=>l.item).length,journalId:journal.id}}});return document;});}
