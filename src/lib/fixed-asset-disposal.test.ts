import{describe,expect,it}from"vitest";import{calculateFixedAssetDisposal}from"./fixed-asset-disposal";describe("fixed asset disposal",()=>{it("calculates a gain",()=>expect(calculateFixedAssetDisposal({originalCost:6500,openingAccumulatedDepreciation:0,postedDepreciation:2000,proceeds:5000})).toEqual({accumulatedDepreciation:2000,netBookValue:4500,gainLoss:500,gain:500,loss:0}));it("calculates a loss",()=>expect(calculateFixedAssetDisposal({originalCost:6500,openingAccumulatedDepreciation:1000,postedDepreciation:2000,proceeds:3000}).loss).toBe(500))});
