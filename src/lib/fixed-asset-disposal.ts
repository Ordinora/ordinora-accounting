export function calculateFixedAssetDisposal(input:{originalCost:number;openingAccumulatedDepreciation:number;postedDepreciation:number;proceeds:number}){const accumulatedDepreciation=Math.min(input.originalCost,Math.max(0,input.openingAccumulatedDepreciation+input.postedDepreciation)),netBookValue=Math.max(0,input.originalCost-accumulatedDepreciation),gainLoss=input.proceeds-netBookValue;return{accumulatedDepreciation,netBookValue,gainLoss,gain:Math.max(0,gainLoss),loss:Math.max(0,-gainLoss)}}
